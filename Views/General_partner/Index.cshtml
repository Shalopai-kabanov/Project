@model IEnumerable<MB.Data.General_partner>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">General Partner List</h2>
        </div>
        @if (User.IsInRole("admin") || User.IsInRole("moderator"))
            {
                <div class="col-6 text-right">
                    <a asp-controller="General_partner" asp-action="Create" class="btn btn-primary">Create new General Partner</a>
                </div>
            }
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        General Partner name
                    </th>
                    <th>
                        Action
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var general_partner in Model)
                {
                <tr>
                    <td width="70%">@general_partner.General_partner_name</td>
                    @if (User.IsInRole("admin") || User.IsInRole("moderator"))
                    {
                        <td width="30%">
                            <div class="w-100 btn-group" role="group">
                                <a asp-controller="General_partner" asp-action="Update" asp-route-Id="@general_partner.Id" class="btn btn-primary mx-1">Update</a>
                                <a asp-controller="General_partner" asp-action="Delete" asp-route-Id="@general_partner.Id" class="btn btn-danger mx-1">Delete</a>
                            </div>
                        </td>
                    }
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No items created yet</p>
    }

</div>

